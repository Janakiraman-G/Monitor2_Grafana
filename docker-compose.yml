services:
  #added prometheus service
  prometheus:
    image: prom/prometheus
    ports:
      - "9091:9091"
    volumes:
        - ./prometheus.yml:/etc/prometheus/prometheus.yml

  # add node exporter service
  node_exporter:
    image: prom/node-exporter:latest
    ports:
      - "9200:9200"
  #add grafana service
  grafana:
    #added grafana service

    image: grafana/grafana:latest

    # added ports for grafana
    ports:
      - "3000:3000"

    #Configure environment variables for Grafana
    # Set the admin username and password for Grafana.
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
    depends_on:
      - prometheus




      

